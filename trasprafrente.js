// Vetor de 5 elementos inteiros
const vetor = [10, 20, 30, 40, 50];

// Estrutura de repetição para imprimir de trás para frente
for (let i = vetor.length - 1; i >= 0; i--) {
    console.log(vetor[i]);
}